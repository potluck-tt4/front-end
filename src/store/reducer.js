const initialState = {
  name: "",
  email: "",
  password: "",
  secondPass: "",
};

export const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
